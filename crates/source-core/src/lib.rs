//! Consolidated core: domain types, abstract ports, and schema contracts.

pub use source_types as types;
pub use source_ports as ports;
pub use source_contracts as contracts;

// Re-export top-level common types for convenience
pub use source_types::*;
