//! Consolidated engine: parse, diagnose, patch, pattern, identify, and adapters.

pub use source_parse as parse;
pub use source_diagnose as diagnose;
pub use source_patch as patch;
pub use source_pattern as pattern;
pub use source_identify as identify;
pub use source_adapters as adapters;
